# Documentation Architecture

## 目录结构

```
docs/
├── index.md            # 文档首页
├── original/           # 用户原创笔记（受控，AI 不得修改）
│   └── ...
├── other-ai/           # AI 辅助内容（自动组织）
│   └── ...
└── [其他 AI 生成的笔记] # 基于 original/ 生成，仅在明确要求时才改动
```

## 内容分区规则

### `docs/original/` — 用户原创笔记

- 这是用户的核心知识资产，所有权归用户。
- 用户自己写或复制粘贴内容。
- **AI 不得修改此目录下的任何文件。**

### `docs/` 下其他笔记 — AI 根据 original/ 生成

- 当有新的代码提交时，AI 根据新增代码和 `original/` 中的内容，生成完整笔记。
- 这些笔记应放在 `docs/` 下，文件名和结构由 AI 根据内容自动组织。
- **仅在用户明确要求时才改动这些文件。**
- 生成后的笔记必须经过用户 **人工确认** 后，才正式提交。

### `docs/other-ai/` — AI 辅助内容

- 存放 AI 提供的帮助性内容，但 **未经用户审查**。
- 包括：AI 自动生成的解释、补充说明、参考链接等。
- 该目录内容仅供参考，不代表用户的最终观点或结论。
- 没有特殊说明时，AI 都应把相关文档有组织地放到这里。

## 写作流程

1. 用户提交新代码。
2. AI 扫描代码变更，结合 `original/` 内容，生成笔记草稿。
3. 与代码变更直接相关的笔记 → 放入 `docs/` 对应位置，等待用户审查后提交。
4. 补充说明、参考链接等辅助内容 → 放入 `docs/other-ai/`，供用户选择性审查。
