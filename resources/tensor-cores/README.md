# Tensor Core 与 CuTe

## 本地入口

- `LeetCUDA/`：按练习推进 WMMA、MMA、CuTe 和部分 Blackwell 内容。

## 学习范围

阶段 2 先建立 FP16 WMMA 的 fragment、warp 协作和布局模型，再进入更底层的 MMA/CuTe。不要跳过 CUDA Core baseline，也不要只根据最终 TFLOP/s 判断是否理解了数据流。

## 使用方式

任务书应指定具体练习路径或关键词，并要求保存上游 commit、编译参数和正确性结果。
